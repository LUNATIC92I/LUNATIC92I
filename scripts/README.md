# Scripts

Developer/operator helper scripts (local bootstrap, synthetic data
generation driver — Phase 36, backup/restore drills — Phase 20, load-test
runners — Phase 18). Empty at Phase 1; each phase adds the scripts it
needs alongside its own acceptance criteria in `docs/DEVELOPMENT_PLAN.md`.
