name: lunatic-siem

# Phase 1 development stack. Images are pinned to exact versions (never
# `latest` — THREAT_MODEL.md / spec §32). Network segmentation approximates
# the trust boundaries in THREAT_MODEL.md §1: the data tier is reachable
# only from the backend, never from the frontend or the host network
# directly (Kubernetes NetworkPolicies formalize this further in Phase 19).

networks:
  public:
  app:
  data:

volumes:
  postgres-data:
  redis-data:
  opensearch-data:
  grafana-data:

services:
  postgres:
    image: postgres:16.4-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-lunatic}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      POSTGRES_DB: ${POSTGRES_DB:-lunatic_siem}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks: [data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lunatic} -d ${POSTGRES_DB:-lunatic_siem}"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 1g
    cpus: 1.0

  redis:
    image: redis:7.4.1-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    networks: [data]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 512m
    cpus: 0.5

  opensearch:
    image: opensearchproject/opensearch:2.17.1
    restart: unless-stopped
    environment:
      discovery.type: single-node
      bootstrap.memory_lock: "true"
      OPENSEARCH_JAVA_OPTS: "-Xms512m -Xmx512m"
      OPENSEARCH_INITIAL_ADMIN_PASSWORD: ${OPENSEARCH_INITIAL_ADMIN_PASSWORD:?OPENSEARCH_INITIAL_ADMIN_PASSWORD must be set}
    ulimits:
      memlock: { soft: -1, hard: -1 }
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    networks: [data]
    healthcheck:
      test: ["CMD-SHELL", "curl -sk -u admin:$${OPENSEARCH_INITIAL_ADMIN_PASSWORD} https://localhost:9200/_cluster/health | grep -q '\"status\"'"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s
    mem_limit: 1.5g
    cpus: 1.5

  opensearch-dashboards:
    image: opensearchproject/opensearch-dashboards:2.17.1
    restart: unless-stopped
    environment:
      OPENSEARCH_HOSTS: '["https://opensearch:9200"]'
    networks: [data]
    depends_on:
      opensearch:
        condition: service_healthy
    mem_limit: 512m
    cpus: 0.5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    networks: [public, app, data]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      opensearch:
        condition: service_healthy
    mem_limit: 1g
    cpus: 1.0

  # Parser worker: events.raw -> parse -> OCSF normalize -> events.normalized.
  # Horizontally scalable (`--scale parser=N`): consumers share one Redis
  # Streams consumer group, so each event is handled exactly once across
  # however many replicas are running.
  parser:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: ["python", "-m", "app.workers.parser_worker"]
    restart: unless-stopped
    env_file: .env
    networks: [app, data]
    depends_on:
      redis:
        condition: service_healthy
    mem_limit: 512m
    cpus: 1.0

  # Syslog collector: a separate deployable from the API so the ingestion
  # hot path never shares a failure domain with request/response traffic
  # (ARCHITECTURE.md §3). Started only when SYSLOG_TENANT_ID and an explicit
  # source allowlist are configured — it refuses to run otherwise, because
  # an unauthenticated transport must never be open to everyone by default.
  syslog-collector:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command:
      ["python", "-m", "app.workers.syslog_collector"]
    restart: unless-stopped
    env_file: .env
    profiles: ["syslog"]
    ports:
      - "${SYSLOG_UDP_PORT:-5514}:5514/udp"
      - "${SYSLOG_TCP_PORT:-5515}:5515/tcp"
    networks: [public, app, data]
    depends_on:
      redis:
        condition: service_healthy
    mem_limit: 512m
    cpus: 1.0

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    restart: unless-stopped
    ports:
      - "5173:80"
    networks: [public]
    depends_on:
      - backend
    mem_limit: 256m
    cpus: 0.5

  prometheus:
    image: prom/prometheus:v2.55.1
    restart: unless-stopped
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    networks: [app]
    depends_on:
      - backend
    mem_limit: 512m
    cpus: 0.5

  grafana:
    image: grafana/grafana:11.3.1
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD must be set}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3000:3000"
    networks: [app]
    depends_on:
      - prometheus
    mem_limit: 512m
    cpus: 0.5
